# ─── RAG Аренда: зависимости проекта ─────────────────────────────────────────
# Установка: pip install -r requirements.txt
# Python: 3.10+
# ─────────────────────────────────────────────────────────────────────────────

# --- OpenAI ---
openai>=1.0.0
tiktoken>=0.7.0

# --- LangChain экосистема ---
langchain-core>=0.2.0
langchain-openai>=0.1.0
langchain-chroma>=0.1.0
langchain-community>=0.2.0
langchain-cohere>=0.1.0
langchain-classic>=1.0.0

# --- Векторная база ---
chromadb>=0.5.0

# --- Cohere (reranker) ---
cohere>=5.0.0

# --- NLP: извлечение сущностей (арендаторы) ---
natasha>=1.6.0

# --- Работа с документами ---
python-docx>=1.1.0          # чтение .docx
openpyxl>=3.1.0             # чтение .xlsx
xlrd>=2.0.1                 # чтение .xls (старый формат Excel)

# --- Данные и аналитика ---
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0         # TSNE для визуализации эмбеддингов

# --- Визуализация ---
plotly>=5.0.0

# --- Утилиты ---
python-dotenv>=1.0.0        # загрузка .env файла
chardet>=5.0.0              # определение кодировки файлов
tqdm>=4.66.0                # прогресс-бар обработки файлов
rank-bm25>=0.2.2            # BM25 поиск по ключевым словам
